# GigaCode Blog

Brandon Martinez's personal blog, announced on LinkedIn 2023-07.

## Resolved URL

**<https://blog.gigacode.dev/>**

Confirmed via `/readme.md` author bio: Brandon (ex-eTop Technology, Arizona → California), `giga@gigacode.dev`, Discord `@gigacode`, Mastodon `@gigacode@infosec.exchange`, Threads `@gigacodedev`. No Twitter.

## Candidates ruled out

- `gigacode.dev` — root, not the blog
- `gigacode.io` — not the blog
- `blog.etoptech.com` — not the blog
- `brandonmartinez.dev` — not the blog
- `gigacodedev.github.io` — not the blog

## Post inventory

Publication dates are not exposed on the public site. Inventory is via `/sitemap.md` and `/llms-full.txt`. The blog is a tree; not every node is a post. "Section" below = parent/TOC page.

### Cybersecurity
- In the Wild — Abusing JWT Encoding (POST) — phishing campaign writeup on JWT-embedded tokens evading URL sandboxing.

### Microsoft Azure → Cognitive AI Services → Speech Services
- Speech to Text (overview post)
- Choosing Transcription Methods (POST) — real-time vs batch guidance
- Provisioning your Cognitive Speech Services (POST)
- Batch Transcription (POST)

### Microsoft Azure → Cost Management
- Intro to Azure Cost Management (POST) — consumption vs reserved pricing
- Automating Azure Cost Management (POST) — runbook that stops VMs on budget alert; Azure Automation + Action Groups

### Microsoft Azure → Bot Framework
- Intro to Bot Framework (POST) — explicitly notes Rewst is the author's preferred RPA platform, promises a follow-up on using a Rewst workflow as the bot messaging endpoint
- Creating your Bot Service (POST) — provisioning Azure Bot Service + Teams integration
- Interacting with your Bot Service's API (POST) — REST-only implementation
- Sending Proactive Messages (POST)
- Packaging Your Bot (POST)

### Zero Trust Networking
- What is Zero Trust (POST)
- Setting Up Your Tenant (POST) — Cloudflare Zero Trust account setup

### People / Human Side of Tech
- Your Mental Bandwidth (POST) — burnout, capacity
- Managing Internal Expectations (POST)
- The Multitasking Myth (POST) — task-switching cost research

## Notable gaps

- No Rewst-specific post yet published. The Bot Framework intro explicitly promises one: "using a Rewst workflow as the Bot Framework messaging endpoint." That is worth watching for; when it publishes, it will be the first public practitioner blog post that integrates Rewst with Bot Framework at depth.
